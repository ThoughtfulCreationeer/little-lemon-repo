import React from "react"

function SpecialsSection(){

    //this is where I should write the code that will fetch the data I need of popularity, dishnames, prices and description to display the cards properly and take us to the promised lands
    
    return (
        <section>
            <h2>Specials</h2>
            {/* Content to be rendered goes here */}
        </section>
    );
};

export default SpecialsSection